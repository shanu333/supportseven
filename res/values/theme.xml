<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="NewLayoutTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="android:windowActionBar">false</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">#2196F3</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">#1976D2</item>
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">#FF4081</item>
        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight and colorSwitchThumbNormal. -->
        <item name="windowActionBar">false</item>
    </style>

    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:background">#FF00FF</item>
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">#ffff</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>

    <style name="CustomActionBarTheme"
           parent="@style/Theme.AppCompat">
        <item name="android:actionBarTabStyle">@style/MyActionBarTabs</item>

        <!-- Support library compatibility -->
        <item name="actionBarTabStyle">@style/MyActionBarTabs</item>
        <item name="windowActionBar">false</item>
    </style>

    <!-- ActionBar tabs styles -->
    <style name="MyActionBarTabs"
           parent="@style/Widget.AppCompat.ActionBar.TabView">
        <!-- tab indicator -->
        <item name="android:background">@drawable/abc_tab_indicator_material</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/abc_tab_indicator_material</item>
    </style>

    <style name="NewToolBarStyle" parent="Theme.AppCompat.NoActionBar">
    </style>


</resources>